export type LoginCredential = {
  email: string;
  password: string;
};

export type AuthCredential = {
  email: string;
};

export type CheckPasswordEmailCredential = {
  email: string;
  password: string;
};
